const Article = () => {
    return(
      <nav>
        <ul>
          <a href="#" > Ссылка раз!</a>
          <a href="#" > Ссылка два!</a>
        </ul>
      </nav>
    );
};

export default Article;